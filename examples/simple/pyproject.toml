[project]
name = "simple-example"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["sci-framework-core"]

[tool.uv.sources]
sci-framework-core = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["backend/simple_example"]

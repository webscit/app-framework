{
  "name": "app-framework",
  "private": true,
  "workspaces": [
    "packages/framework-core-ui",
    "examples/frontend"
  ],
  "scripts": {
    "dev": "npm run dev -w @app-framework/examples-frontend",
    "build": "npm run build --workspaces --if-present",
    "format:check": "prettier --check .",
    "lint": "eslint packages examples/frontend",
    "typecheck": "tsc -b packages/framework-core-ui/tsconfig.json examples/frontend/tsconfig.json",
    "test:ui": "npm run test -w @app-framework/core-ui -- --run && npm run test -w @app-framework/examples-frontend -- --run",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "@eslint/js": "^9.39.4",
    "eslint": "^9",
    "prettier": "^3",
    "typescript": "^5",
    "typescript-eslint": "^8.58.1"
  }
}
